"use client";
import { FlaskConical, Play as PlayIcon } from "lucide-react";

export function FlatsIcon() {
  return <FlaskConical className="size-5 text-accent-primary" />;
}
export const Play = PlayIcon;
